# NIIMBOT N1 — драйвер этикеток для Linux

[![Go](https://img.shields.io/badge/Go-1.24+-00ADD8?logo=go)](https://go.dev)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

**Другие языки:** [English](README.en.md) · [Nederlands](README.nl.md) · [简体中文](README.zh-CN.md) · [Pirate 🏴‍☠️](README.pirate.md)

Драйвер термотранспортного принтера этикеток **NIIMBOT N1** по Bluetooth LE.
Написан на Go, работает из командной строки: печатает текст и картинки,
показывает состояние принтера, умеет отрисовать макет без печати.

Никакого приложения вендора, никакого телефона и никаких аккаунтов — только
Linux, Bluetooth и одна команда.

```console
$ niimbot text "1 гульден" "1734"
```

## Возможности

| Команда | Что делает |
|---|---|
| `niimbot info` | состояние принтера и вставленных расходников: id модели, серийный номер, прошивка, заряд, тип этикеток, остаток в рулоне и в ленте |
| `niimbot rfid` | прочитать метки рулона этикеток и ленты (RFID) |
| `niimbot text "строка" "..."` | напечатать текст; каждая строка текста — отдельный аргумент командной строки |
| `niimbot image file.png` | напечатать картинку |
| `niimbot preview "строка"` | отрисовать макет **без печати** — посмотреть, что уйдёт на этикетку |
| `niimbot preview --file img.png` | макет из картинки: видно, что останется после вписывания и порога |
| `niimbot testpage` | встроенная тестовая страница принтера (проверка связи) |
| `niimbot gui` | графический интерфейс в браузере: **серии этикеток**, шаблоны, предпросмотр |
| `niimbot scan` | найти принтер в эфире |

## Требования

- **Linux** с BlueZ (проверено на Debian и Ubuntu; связь с BlueZ идёт через D-Bus);
- **Go 1.24+** для сборки;
- шрифт с кириллицей (DejaVu, Noto или Liberation — ищутся автоматически);
- принтер **NIIMBOT N1** (для других моделей нужны свои параметры головки — см. «Протокол»).

## Установка

```bash
go install github.com/petrovich811/niimbot@latest
```

Или из исходников:

```bash
git clone https://github.com/petrovich811/niimbot.git
cd niimbot
go build -o niimbot .
```

## Использование

```bash
# посмотреть состояние принтера
./niimbot info

# макет без печати (сохраняется в /tmp/niimbot_label_preview.png)
./niimbot preview "1 гульден" "1734"

# печать текста
./niimbot text "1 гульден" "1734"

# картинка
./niimbot image logo.png

# темнее и по две копии — на холдер и в каталог
./niimbot text "2 гульдена 1785" "Утрехт" --density 3 --copies 2
```

### Флаги

| Флаг | Значение | По умолчанию |
|---|---|---|
| `--address MAC` | адрес принтера; без него поиск по имени `N1-` | авто |
| `--length мм` | длина этикетки вдоль подачи | `30` |
| `--font точек` | кегль; `0` — подобрать под размер этикетки | `0` |
| `--density 1..3` | плотность печати | `2` |
| `--label` | тип этикетки; **пусто — определить по метке рулона** | авто |
| `--copies N` | число копий | `1` |
| `--flip` | перевернуть содержимое на 180° | выкл. |
| `-v=false` | без подробного вывода протокола | вкл. |

## Графический интерфейс

```bash
./niimbot gui              # поднимет http://127.0.0.1:8765 и откроет браузер
./niimbot gui --port 9000  # другой порт
./niimbot gui --no-browser # только поднять сервер
```

Интерфейс **встроен в сам бинарник** (`embed` + `net/http`), никаких
GUI-фреймворков и внешних зависимостей. Логика печати не дублируется:
интерфейс вызывает те же функции, что и командная строка.

| Что там есть | Зачем |
|---|---|
| **Вкладка «Серия»** | список этикеток — по одной в строке; печать идёт одним заданием, подряд |
| **Загрузка CSV** | из файла берётся первый столбец (Excel сохраните как CSV) |
| **Предпросмотр** | макет этикетки до печати — в файле видно, как ляжет текст |
| **Шаблоны** | сохранённые наборы настроек под типовые задачи: кабельные бирки, шильдики |
| **Состояние** | модель, серийный номер, заряд, тип этикеток, остаток в рулоне и ленте |
| **Прогресс** | «напечатано N из M» по ходу серии |

Разделитель `|` в строке переносит текст на новую строку внутри одной этикетки:
`1 гульден|1734` даст две строки на этикетке: номинал и год.

Шаблоны хранятся в `~/.config/niimbot/templates.json`.

### Серия многострочных этикеток: шаблон + данные

Если каждая этикетка серии должна быть из нескольких строк, есть **шаблон
этикетки** и **таблица данных** — как в настоящих программах для этикеток.

**Шаблон** (поле «Шаблон этикетки»):

```
{1}
{2}
{3}
```

**Данные** (список, одна запись в строке, столбцы через `;`):

```
Зап. Фрисландия;1 гульден 1734;XF
Голландия;1 гульден 1762;VF
Утрехт;2 гульдена 1785;UNC
```

Напечатается **три этикетки по три строки** на каждой:
`{1}`, `{2}`, `{3}` — это столбцы записи.

| Правило | Как работает |
|---|---|
| Строки шаблона | каждая строка шаблона = строка на этикетке; пустые строки в конце отбрасываются |
| Столбцы | разделитель `;`, `,` или табуляция — что встретится первым |
| Повтор столбца | `ТЕГ {2}` — столбец можно ставить в любом месте строки |
| Без шаблона | одна строка данных = одна этикетка, а символ `\|` внутри переносит текст |

Так CSV из Excel подходит напрямую: загрузили файл, задали шаблон — и вся
таблица ушла на печать серией.

### Печать серии из Excel

1. В Excel сделайте таблицу: **одна строка — одна этикетка**, столбцы — то, что
   попадёт на бирку.
2. Сохраните как **CSV**: «Файл» → «Сохранить как» → CSV.
3. В интерфейсе на вкладке «Серия» нажмите **Загрузить CSV** — строки попали в список.
4. Задайте **шаблон этикетки** со столбцами `{1}`, `{2}`… — он определяет, как
   выглядит этикетка.
5. Посмотрите **Предпросмотр** и нажмите **Печать**.

Галочка «первая строка — заголовки» отбрасывает шапку таблицы. Кавычки защищают
разделитель внутри поля: `"KM# 90; Verkade 45.2";XF` — это два столбца, а не три.

**Про столбцы.** Их должно быть **не меньше**, чем самое большое число в шаблоне:
шаблон с `{3}` требует трёх столбцов. Excel дополняет пустые ячейки сам, поэтому
`Зап. Фрисландия;;XF` — это три столбца, и на этикетке останется **пустая строка**:
раскладка у всех этикеток серии одинаковая. Лишние столбцы игнорируются. Если
столбцов меньше, драйвер **откажется печатать** и скажет, какого не хватает —
вместо того чтобы напечатать на этикетке литеральное `{3}`.

**Порядок столбцов.** Если в таблице столбцы идут не в том порядке, как нужно на
этикетке, переставлять их в Excel не обязательно — впишите номера столбцов сверху
вниз, через запятую:

```
4, 1, 2, 5
```

Это значит: первая строка этикетки — столбец 4, вторая — 1, дальше 2 и 5. Столбец,
который не упомянут (здесь третий), на этикетку не попадёт; один и тот же столбец
можно указать дважды. Поле «Шаблон этикетки» заполнится само — `{4}`, `{1}`, `{2}`,
`{5}` — и его можно потом дополнить, например `ТЕГ {2}`.

## Конструктор этикеток

Открывается ссылкой **«Конструктор этикеток»** в шапке интерфейса или напрямую:
`http://127.0.0.1:8765/designer.html`.

Этикетка собирается из **элементов** — надписей и картинок, у каждого свои
координаты в миллиметрах. Координаты идут в ориентации чтения:

- **X** — вдоль этикетки (по подаче), 0 слева;
- **Y** — поперёк этикетки (по головке), 0 сверху, всего 12 мм.

| Что можно | Как |
|---|---|
| Двигать элементы | тянуть мышью; координаты прилипают к 0,5 мм |
| Задать точно | поля X, Y, ширина, высота в панели справа |
| Надпись | текст с полями `{1}`, `{2}`…, кегль в мм, выравнивание влево/по центру/вправо |
| Картинку | «+ Картинка»: файл встраивается в шаблон целиком |
| Посмотреть результат | кнопка «Обновить макет» — рядом с холстом настоящий оттиск |
| Напечатать серию | список этикеток (вставить или из CSV) и «Напечатать серию» |
| Сохранить | «Сохранить» в разделе «Шаблон» |

**Шаблон остаётся одним файлом**: картинки встраиваются в него как `data:URL`,
поэтому его можно перенести на другой компьютер целиком.

Холст размечен в миллиметрах: тонкие линии — 1 мм, жирные — 5 мм, сверху линейка.
Галочка «сетка 1 мм» убирает разметку.

Помните, что **1 мм — это 8 точек**. На этикетке 14×30 всего 240×96 точек,
поэтому четыре строки кеглем больше 2,5 мм на неё уже не влезут.

### Шрифт и высота строки

У каждой надписи свой шрифт: **семейство**, **жирность** и **высота строки**.

| Поле | Что задаёт |
|---|---|
| **Шрифт** | семейство из системных: «Arial», «DejaVu Sans», «Times New Roman», «Comic Sans MS»… |
| **Кегль, мм** | высота букв |
| **Высота строки, мм** | расстояние между строками внутри одной надписи; `0` — по метрикам шрифта |
| **Жирный** | жирное начертание, если оно есть у семейства |

Список шрифтов берётся **из системы**: Linux (`/usr/share/fonts`, `~/.fonts`,
`~/.local/share/fonts`) и Windows (`C:\Windows\Fonts`, а также шрифты текущего
пользователя). В выпадающем списке — только семейства, **где есть кириллица**:
на этикетках почти всегда русский текст. Полный список отдаёт `/api/fonts`.

В шаблоне хранится **название семейства**, а не путь к файлу: шаблон переживёт
перенос на другую машину, где тот же шрифт лежит в другом месте.

Если названного шрифта в системе нет, драйвер **откажется печатать** и скажет,
какого шрифта не хватает. Молча подставить другой нельзя: этикетка вышла бы не
такой, как задумано, и заметил бы это только человек, глядя на бумагу.

### Линии, рамки и поворот

| Элемент | Что даёт |
|---|---|
| **Линия** | залитая полоса: разделитель между строками или сплошная плашка |
| **Рамка** | контур по краям, толщина задаётся (по умолчанию 0,3 мм) |
| **Поворот надписи** | `0°`, `90°`, `180°`, `270°` — для кабельных бирок, где текст идёт поперёк |

У повёрнутой надписи ширина (`w`) — это **длина текста**, а не ширина рамки:
после поворота она становится высотой блока. На холсте конструктора поворот
показан честно: при 90° и 270° ширина и высота меняются местами.

**Прилипание к краям.** При перетаскивании элемент притягивается не только к
сетке 0,5 мм, но и к **краям и середине** этикетки — на холсте появляются
красные линии-подсказки. Клавиша **Alt** отключает прилипание к краям, если
нужно поставить элемент «на глазок».

### Химические структуры по SMILES

Вид элемента **`smiles`**: строка SMILES, размеры в мм и толщина связей.

```json
{"kind": "smiles", "x": 0.6, "y": 1.2, "w": 9.5, "h": 9.5,
 "smiles": "CC(=CCCC(C)(C=C)O)C", "thickness": 2}
```

SMILES можно брать из данных: `"smiles": "{4}"` — тогда структура подставляется
из четвёртого столбца строки, и серия этикеток выходит со своими структурами.

**Рисует RDKit.** Свой рендер структур мы не пишем: разбор SMILES — это полдела,
а 2D-раскладка (восприятие циклов, расстановка координат, борьба с наложениями)
делается в RDKit годами. Драйвер зовёт его и получает готовый PNG.

#### Что наше, а что чужое

Структуры — **единственное место**, где драйвер опирается на сторонний код.
Граница проходит так:

| Делает наш драйвер (Go) | Делает RDKit (Python) |
|---|---|
| вид элемента `smiles` в шаблоне | разбор строки SMILES |
| размеры в мм, поворот, толщина связей | 2D-раскладка: циклы, координаты, наложения |
| подстановка столбца данных: `"smiles": "{4}"` | отрисовка молекулы в PNG |
| кэш: одна молекула рисуется раз на серию | |
| поиск Python с RDKit, понятные ошибки | |

**Почему не пишем сами.** Разбор SMILES — это полдела и примерно неделя работы.
Настоящая сложность — **2D-раскладка**: понять, где цикл, как расставить атомы,
чтобы связи не пересекались и не наезжали друг на друга. На это RDKit и Open Babel
потратили годы, и своя реализация вышла бы заметно хуже.

**Лицензии.** Ядро RDKit — **BSD 3-Clause**, обёртка пакета `rdkit` на PyPI — **MIT**.
Обе разрешительные и совместимы с нашей MIT. RDKit **не входит в поставку**: он
ставится отдельно, поэтому лицензия драйвера не затрагивается.

#### Установка RDKit

```bash
python3 -m venv ~/.local/share/niimbot/chemvenv
~/.local/share/niimbot/chemvenv/bin/pip install rdkit
```

Права root не нужны. Драйвер ищет Python в таком порядке: переменная
`NIIMBOT_RDKIT`, каталог `chemvenv` рядом с бинарником, `~/.local/share/niimbot/chemvenv`,
затем системный `python3`. Если RDKit не найден, печать структур откажет
с понятным сообщением, а остальное продолжит работать.

#### Что важно знать про размер

Этикетка 14×30 — это всего **240×96 точек**. Структуру надо рисовать **сразу
в нужном размере** и **толстыми связями**: скачанная картинка структуры при
уменьшении до 96 точек рассыпается в отдельные точки. Проверено:

| Молекула | Тяжёлых атомов | В квадрате 96×96 | На всю ширину |
|---|---|---|---|
| ванилин, кумарин | 9 | читается | читается |
| линалоол, геранiol | 10–11 | читается | читается |
| мускус-кетон | 15 | читается | читается |
| холестерин | 28 | еле угадывается | **читается** |

Крупные молекулы выручает **вся ширина этикетки** (230 точек вместо 96).
Подписи гетероатомов (O, N) на 8 точках/мм теряются — буква занимает около
6 точек. Стереохимия (клинья) на таком размере неразличима.

#### Поворот структуры

Высота этикетки всего 12 мм, поэтому квадратная структура упирается в неё.
Поворот снимает это ограничение: у элемента есть поле `rotate`, и повёрнутая
структура может занять **всю длину этикетки**.

```json
{"kind":"smiles","x":0.3,"y":0.5,"w":18,"h":11,"smiles":"{3}","thickness":2,"rotate":90}
```

Поворачивает **сам RDKit** (опция `rotate`), а не мы после отрисовки: он
раскладывает молекулу под нужную рамку, а не вписывает её в квадрат и крутит.
Поворот работает и для картинок.

**Но есть предел, и он на бумаге.** На этикетке 14×30 большая структура и
крупный текст вместе не помещаются. Проверено тремя раскладками:

| Раскладка | Структура | Что вышло |
|---|---|---|
| 9,5×9,5 мм, текст 18 мм | мелкая | читаемо, но структура невелика |
| 18×11 мм **с поворотом**, текст 11 мм | крупная | **название обрезалось** — столбца не хватило |
| **15×10 мм, текст 14 мм** | крупная | и структура крупная, и текст целиком |

Остановились на третьей. Если название длиннее «ЛИНАЛООЛ» — уменьшайте кегль
или отдавайте структуре больше места в конструкторе.

#### Готовый шаблон серии

В настройках сохранён шаблон **«Парфюмерные ингредиенты»**. Данные — CSV:

```
название;CAS;SMILES;нота
ЛИНАЛООЛ;78-70-6;CC(=CCCC(C)(C=C)O)C;цветочный, свежий
КУМАРИН;91-64-5;O=c1ccc2ccccc2o1;сладкий, сенный
ВАНИЛИН;121-33-5;COc1cc(C=O)ccc1O;ванильный, бальзамический
```

| Что на этикетке | Откуда |
|---|---|
| структура | столбец 3 (SMILES) |
| название | столбец 1, жирным |
| CAS | столбец 2, всегда |
| нота | столбец 4 |
| дата и время | подставляются при печати |

### Порог чёрно-белого

Принтер печатает **только чёрное и белое**, поэтому серое изображение перед
отправкой переводится по порогу: всё, что темнее порога, становится чёрным.

Порог по умолчанию **200** из 255, и это важнее, чем кажется. Линии и текст
рисуются со сглаживанием, их края светло-серые; при пороге 128 связи структур
выходили **пунктиром**, а цифры в CAS рассыпались на точки. Проверено на бумаге:

| Порог | Что выходит |
|---|---|
| 128 | связи рвутся, мелкий текст рассыпается |
| **200** | плотные линии, целый текст — по умолчанию |
| 230 и выше | буквы начинают заплывать |

Меняется тремя способами: флаг `--threshold 1..255`, поле «Порог чёрно-белого»
в конструкторе, поле `threshold` в запросах `/api/preview` и `/api/print`.

**Макет в конструкторе показывается уже чёрно-белым** — ровно таким, каким уйдёт
в принтер, а не в оттенках серого, как было раньше. Поэтому бледные линии видно
ещё до печати.

### Дата и время печати

В любой надписи работают поля, подставляемые **в момент печати**:

| Поле | Что даёт |
|---|---|
| `{дата}` | `18.09.2026` |
| `{время}` | `00:25` |
| `{дата-время}` | `18.09.2026 00:25` |

В конструкторе для этого есть кнопка **«+ Дата и время»**: она ставит надпись
мелким шрифтом у нижнего края. Подходит для этикеток ингредиентов: видно,
когда смесь составлена или вскрыта.

### Шаблон как данные

Тем, кто работает через API, шаблон понятен и без конструктора:

```json
{
  "length": 30,
  "elements": [
    {"kind": "text", "x": 1, "y": 0.5, "w": 13, "text": "лот {4}", "font": 2.2, "align": "left"},
    {"kind": "text", "x": 16, "y": 0.5, "w": 13, "text": "{5}", "font": 2.2, "align": "right"},
    {"kind": "text", "x": 1, "y": 3.2, "w": 28, "text": "{1}", "font": 2.6},
    {"kind": "image", "x": 20, "y": 3, "w": 8, "h": 8, "image": "data:image/png;base64,…"}
  ]
}
```

Тот же список принимают `/api/preview` и `/api/print` в поле `elements`.

## Этикетка с картинкой

```bash
# посмотреть, что уйдёт на печать, не тратя этикетку
./niimbot preview --file эскиз.png

# напечатать
./niimbot image эскиз.png
```

Форматы: **PNG, JPEG, GIF**. Картинка любого размера **вписывается** в область
этикетки целиком, с сохранением пропорций, и центрируется на белом поле —
подгонять пиксели вручную не нужно.

### Рисуйте сразу в размер этикетки

Принтер печатает **203 dpi**, то есть **8 точек на миллиметр**. Область этикетки:

| Этикетка | Размер картинки |
|---|---|
| 14×30 мм | **240×96** точек |
| 14×40 мм | 320×96 |
| 14×50 мм | 400×96 |

Высота всегда **96** точек — это ширина печатающей головки (12 мм), а ширина
равна длине этикетки в миллиметрах, умноженной на 8.

**Вписывание работает, но лучше рисовать сразу в этом размере.** Если нарисовать
900×360, текст уменьшится почти вчетверо и станет рыхлым: разрешение этикетки —
всего 240×96, запаса нет.

### В чём рисовать

| Программа | Когда удобна |
|---|---|
| **Inkscape** | лучшее для этикеток: документ 240×96 пикселей, фигуры и текст, экспорт в PNG |
| **GIMP** | если нужна фотография: масштаб до 8 пикселей на миллиметр, оттенки серого |
| **LibreOffice Draw** | если привычнее офисный редактор и хватает простых фигур |
| **ImageMagick** | из командной строки: `convert исходник.png -resize 240x96! макет.png` |
| любая другая | важно одно: на выходе PNG нужного размера |

Печать **чёрно-белая**: серый превращается в чёрный или белый по порогу, поэтому
полутона и тонкие линии пропадают. Проверяйте макетом `preview --file` — он
показывает ровно то, что уйдёт в принтер.

## Этикетки и ориентация

### Виды этикеток

Протокол NIIMBOT знает восемь типов этикеток, но **N1 поддерживает пять**
([описание типов](https://printers.niim.blue/other/label-types/)):

| Код | Тип | N1 |
|---|---|---|
| 1 | с зазорами — обычные (`withgaps`) | ✅ |
| 2 | чёрные термочувствительные (`black`) | ❌ |
| 3 | непрерывная лента (`continuous`) | ✅ |
| 4 | перфорированные (`perforated`) | ❌ |
| 5 | прозрачные (`transparent`) | ✅ |
| 6 | ПВХ-бирки (`pvctag`) | ❌ |
| 10 | с чёрной меткой (`blackmarkgap`) | ✅ |
| 11 | термоусадочная трубка (`heatshrink`) | ✅ |

В протоколе есть типы `black`, `perforated` и `pvctag`, но прошивка N1 их
не поддерживает. Драйвер проверяет тип **до подключения** к принтеру и отказывается
печатать, если модель его не поддерживает.

Для N1 продаются этикетки шириной **до 15 мм** (печатаемая часть — 12 мм
по центру): белые матовые 14×30, 14×40 и 14×50 мм, матовое серебро и
прозрачные 14×30 мм, кабельные 12,5×109 мм четырёх цветов. Печать
термотранспортная — нужна красящая лента (риббон).

### Автоопределение расходников

В каждом рулоне — и этикеток, и ленты — стоит **RFID-метка**. Принтер читает
её сам, а драйвер умеет спросить:

```console
$ niimbot rfid

=== рулон этикеток ===
  UUID:                  881dcce946121080
  штрихкод:              12242117
  серийный номер:        PC0H902384002378
  тип этикетки:          withgaps (1) — N1 поддерживает
  ресурс:                228, израсходовано 43, осталось 185

=== лента (риббон) ===
  UUID:                  881d8209fa900000
  серийный номер:        PZ1GA06304000391
  ресурс:                1600, израсходовано 901, осталось 699
```

**Тип этикетки определяется автоматически.** Если `--label` не задан, драйвер
читает метку рулона и берёт тип оттуда: принтер знает, что в него вставлено,
лучше любой догадки. Если тип из метки модель не поддерживает, печать не
начнётся. Если `--label` задан явно и расходится с меткой, драйвер предупредит:

```
внимание: в принтере этикетки типа withgaps, а печатаем как continuous
```

Длину этикетки метка тоже хранит, но принтер **не отдаёт её хосту** (вендорное
приложение берёт размеры с сервера по номеру рулона), поэтому длину по-прежнему
задают флагом `--length`.

### Ориентация

Принтер N1 печатает головкой на **96 точек** — это **12 мм** при 203 dpi.
Здесь используются этикетки **EW14×30**: 14 мм по ширине и 30 мм по длине
вдоль подачи. Печатаемая часть — 12 мм по центру этикетки.

**Текст идёт вдоль этикетки.** Чтобы так получилось, содержимое рисуется
в «ориентации чтения». В ней ширина картинки — это длина этикетки,
а высота — ширина головки. Дальше картинка превращается в строки принтера:

- **транспонируется** — ось X картинки становится направлением подачи;
- **ось головки идёт в обратном порядке** — точка головки `c` берётся
  из строки картинки как `across-1-c` (в NiimBlueLib это
  `idx = (height-1-col)*width + row`).

Обе тонкости проверены печатью на живом принтере, и обе были ошибками автора:

1. если рисовать «как на экране», текст ложится **поперёк** этикетки;
2. без разворота оси головки этикетка выходит **зеркальной**.

Если этикетка печатается перевёрнутой, добавьте `--flip` — это поворот на 180°.

## Протокол

Пакет:

```
55 55 | команда | длина | данные | XOR-контроль | AA AA
```

Многобайтовые числа — **big-endian**. Контрольная сумма — XOR команды,
длины и всех байт данных.

Цикл печати:

```
плотность (0x21) → тип этикетки (0x23) → старт печати (0x01) →
старт страницы (0x03) → размер страницы (0x13) → строки битмапа (0x85) →
конец страницы (0xE3) → опрос статуса (0xA3) → конец печати (0xF3)
```

Строка битмапа: `позиция(2) | счётчики чёрных точек(3) | повторы(1) | данные(12)`,
где бит 7 первого байта — крайняя точка головки. Полностью белые строки
отправляются отдельной командой `0x84` — это короче и быстрее.

Статус печати (ответ `0xB3`): `страница(2) | прогресс печати % | прогресс подачи %`.
Печать завершена, когда оба прогресса равны 100.

### Две тонкости серийной печати

Обе найдены на живом принтере и обе стоили сорванных этикеток:

**1. Между строками битмапа нужна пауза** (здесь — 4 мс). `writeWithoutResponse`
отдаёт данные быстрее, чем принтер успевает их принять, и он отвечает ошибкой
данных: ответ `0xDB`, код `6`. Без паузы срывается уже вторая этикетка серии.

**2. Между этикетками нужно ждать завершения страницы.** Пока принтер печатает
предыдущую этикетку, следующая для него — лишние данные, и он снова отдаёт
ошибку `0xDB`/`6`. Драйвер опрашивает статус и ждёт двух подряд замеров
«100 % печати и подачи», только потом шлёт следующую.

Полезно и то, что **ошибка `0xDB` читается**: драйвер расшифровывает код
(открыта крышка, нет бумаги, перегрев, нет ленты и так далее) и прекращает
задание, вместо того чтобы молча ждать таймаута.

### Две ловушки, на которые стоит обратить внимание

**1. У принтера два BLE-сервиса, и рабочий только один.**

| Сервис | Что это |
|---|---|
| `e7810a71-73ae-499d-8c15-faa9aef0c3f2` | **родной сервис NIIMBOT** — его слушает прошивка, характеристика `bef8d6c9-9c21-4c9e-b632-bd58c1009f9f` |
| `49535343-fe7d-4ae5-8fa9-9fafd205e455` | прозрачный UART — подходит по свойствам (`notify` + `write`), но **на команды не отвечает вообще** |

Подключение к UART выглядит успешным, но принтер молчит: команды уходят
«в пустоту». Именно из-за этого при отладке тратится больше всего времени.

**2. BlueZ нужно сначала «увидеть» устройство.**

Без предварительного сканирования подключение падает с ошибкой D-Bus:

```
Method "Get" with signature "ss" on interface "org.freedesktop.DBus.Properties" doesn't exist
```

Плюс важная деталь для Go: `Adapter.Scan` у Linux-бэкенда библиотеки
`tinygo.org/x/bluetooth` **блокируется до `StopScan`** — его нужно запускать
в отдельной горутине, иначе программа зависнет.

### Другие модели

Параметры привязаны к N1: id модели `3586`, головка `96` точек, плотность `1..3`.
Для другой модели нужно поменять эти значения в `protocol.go`
(таблица соответствий «модель → параметры» есть в
[NiimBlueLib](https://github.com/MultiMote/niimbluelib/blob/master/src/printer_models.ts)).

## Тесты

```bash
go test ./...
```

Тесты проверяют сборку пакетов **сверкой с байтами, снятыми с живого принтера**
(рукопожатие `5555c10101c1aaaa`, запрос модели `555540010849aaaa`), разбор
ответов, склейку неполных пакетов, счётчики точек, ориентацию строк
и белый фон холста.

## Что проверено, а что нет

**Проверено на живом принтере N1 (GA24110447):**

- поиск, подключение и рукопожатие;
- id модели `3586`, серийный номер, прошивка `3.13`, заряд;
- **печать текста** — полный цикл: `страница 1, печать 100%, подача 100%`;
- **печать картинки** — рамка, текст и метка в углу легли так же, как на макете;
- **ориентация** — оттиск читается правильно: текст вдоль этикетки,
  не зеркальный. Подтверждено на бумаге после двух исправлений,
  описанных выше;
- **серия из нескольких этикеток одним заданием** — три этикетки напечатаны
  подряд, `печать завершена (страница 3, 100%)`;
- **порядок столбцов** — на бумаге строки идут именно в заданном порядке
  (`4, 1, 2, 5`), а пропущенный столбец на этикетку не попадает;
- **пустой столбец оставляет пустую строку** — у всех этикеток серии
  одинаковое число строк, раскладка не съезжает;
- **конструктор этикеток** — серия по шаблону из размещённых элементов
  напечатана: текст встаёт по координатам, выравнивание действует;
- **шрифт и высота строки** — четыре шрифта (Arial, Times New Roman,
  Comic Sans MS, DejaVu Sans) различимы на бумаге, а шаг строки 4 мм
  заметно раздвигает строки;
- **рамка, разделитель и поворот надписи на 90°** — напечатаны вместе на одной
  этикетке: рамка целая со всех сторон, разделитель ровный, повёрнутый текст
  читается при наклоне головы;
- **плотность печати** — все три ступени (1, 2 и 3) дают годный оттиск, включая
  тонкую линию 0,2 мм и мелкий текст 1,6 мм; лучшей признана **2** — она и стоит
  по умолчанию;
- **картинка, нарисованная сразу в размер этикетки, чётче**, чем та же
  картинка, уменьшенная из крупного холста. Сравнение на бумаге: 240×96
  против 900×360 — первая заметно лучше. Поэтому правило «рисуйте в размер»
  не придирка, а следствие разрешения 203 dpi;
- ошибки ориентации и подключения обнаружились именно на живом принтере:
  ни тесты, ни макет их не показывали.

**Пока не проверено:**

- печать на других типах этикеток (`--label`), кроме `withgaps`;
- другие модели NIIMBOT (нужны свои параметры головки).

## Сборка под другие системы

Драйвер написан на Go, поэтому собирается под Linux, Windows и macOS:

```bash
go build -o niimbot .                                  # своя система
GOOS=windows GOARCH=amd64 go build -o niimbot.exe .    # Windows, из Linux тоже
GOOS=darwin  GOARCH=arm64 go build -o niimbot-mac .    # macOS — ТОЛЬКО на Mac
```

| Система | Состояние |
|---|---|
| **Linux** | работает, проверено на живом принтере |
| **Windows** | собирается из Linux без правок; на живом принтере пока не проверялось |
| **macOS** | **собирать нужно на Mac**: там Bluetooth — это CoreBluetooth, а её привязка требует CGO и фреймворков Apple. Кросс-сборка из Linux невозможна |

**Особенность macOS:** система не отдаёт настоящие Bluetooth-адреса — вместо MAC выдаёт UUID. Поэтому на Mac принтер ищется сканированием, а `--address` принимает UUID, а не MAC.

**Шрифт** ищется сам: DejaVu/Noto/Liberation в Linux, Arial/Segoe UI в Windows, Arial/Helvetica в macOS. Свой можно задать переменной `NIIMBOT_FONT`.

## Смежные проекты

Если Linux рядом с принтером нет, есть и другие пути:

| Проект | Что даёт |
|---|---|
| [NiimBlueLib](https://github.com/MultiMote/niimbluelib) | открытая библиотека протокола NIIMBOT (JS, MIT) — на ней построен и этот драйвер |
| [NiimBlue](https://niim.blue/) | готовый веб-клиент: печать из браузера по Web Bluetooth, **без сервера**. На Android работает в Chrome, на iPhone нужен браузер [Bluefy](https://apps.apple.com/us/app/bluefy-web-ble-browser/id1492822055) |
| [NiimPrintX](https://github.com/labbots/NiimPrintX) | клиент на Python с графическим интерфейсом; модели D11, B21, B1, D110, B18 — N1 в списке нет |

Этот драйвер отличается тем, что работает из командной строки на Linux и умеет
**серии этикеток из CSV с шаблоном**.

**Химические структуры** рисует **[RDKit](https://www.rdkit.org/)** (BSD 3-Clause) —
он вызывается как отдельная программа и в поставку драйвера не входит. Всё
остальное в этом драйвере — свой код на Go.

## Благодарности

Протокол восстановлен по открытой библиотеке
**[NiimBlueLib](https://github.com/MultiMote/niimbluelib)** (MIT) — самой точной
открытой реализации протокола NIIMBOT. Оттуда взяты формат пакетов,
коды команд, разбиение счётчиков точек и параметры моделей.
Код этого драйвера написан с нуля на Go.

## Дисклеймер

Проект не связан с NIIMBOT и не одобрен производителем. «NIIMBOT» и названия
моделей принадлежат их владельцам. Взаимодействие с принтером идёт по
восстановленному протоколу — используйте на свой риск.

## Лицензия

[MIT](LICENSE).
